/** Built-in Agent Source identities are shared with the renderer-safe core
 * contract. Electron keeps only PTY-specific composition here. */
import type {
  AgentHarness,
  AgentPermissionMode,
  PtyHarness,
} from '@exawatt/core';

export type { AgentHarness, AgentPermissionMode, PtyHarness };
